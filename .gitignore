# Rust build output
/target/

# Local debug / trace from running the CLI
trace.jsonl

# Vendored Tidal tree — not used by `cargo build`; clone upstream if you need it
tidal-source/

# Secrets
.env
.env.*

# macOS
.DS_Store

